package com.ar.mylapp.models.cardProperties

class CollectionDTO(
    id: Int,
    name: String
) : CardPropertiesDTO(id, name)

class FormatDTO(
    id: Int,
    name: String
) : CardPropertiesDTO(id, name)

class RaceDTO(
    id: Int,
    name: String
) : CardPropertiesDTO(id, name)

class RarityDTO(
    id: Int,
    name: String
) : CardPropertiesDTO(id, name)

class TypeDTO(
    id: Int,
    name: String
) : CardPropertiesDTO(id, name)

class KeyWordDTO(
    id: Int,
    name: String,
) : CardPropertiesDTO(id, name)
